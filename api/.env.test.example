# Copy to .env.test and adjust. Used by: yarn test:integration:local
# (Run yarn setup-test-db first to start the Docker test DB on port 5433.)
# After DB schema changes (e.g. renamed functions), re-run db/init scripts against the test DB (see db/README.md).

TEST_DB_HOST=localhost
TEST_DB_PORT=5433
TEST_DB_NAME=pm_test
TEST_DB_USER=pm_user
TEST_DB_PASSWORD=pm_password
SESSION_SECRET=test
