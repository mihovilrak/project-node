<!DOCTYPE html>
<html>
<head>
    <style>
        body { font-family: Arial, sans-serif; line-height: 1.6; }
        .container { max-width: 600px; margin: 0 auto; padding: 20px; }
        .header { background-color: #2196f3; padding: 20px; text-align: center; color: white; }
        .content { padding: 20px; }
        .button {
            display: inline-block;
            padding: 10px 20px;
            background-color: #2196f3;
            color: white;
            text-decoration: none;
            border-radius: 5px;
        }
    </style>
</head>
<body>
    <div class="container">
        <div class="header">
            <h2>Project Update</h2>
        </div>
        <div class="content">
            <p>Hello {{userName}},</p>
            <p>There has been an update to a project you're involved with.</p>
            {{#if projectName}}
            <p>Project Details:</p>
            <ul>
                <li>Project: {{projectName}}</li>
                {{#if updateType}}<li>Update: {{updateType}}</li>{{/if}}
                {{#if updatedBy}}<li>Updated by: {{updatedBy}}</li>{{/if}}
            </ul>
            {{/if}}
            {{#if message}}
            <p><strong>Details:</strong> {{message}}</p>
            {{/if}}
            <p>
                <a href="{{taskUrl}}" class="button">View Project</a>
            </p>
        </div>
    </div>
</body>
</html>
