<!DOCTYPE html>
<html>
<head>
    <style>
        body { font-family: Arial, sans-serif; line-height: 1.6; }
        .container { max-width: 600px; margin: 0 auto; padding: 20px; }
        .header { background-color: #4caf50; padding: 20px; text-align: center; color: white; }
        .content { padding: 20px; }
        .button {
            display: inline-block;
            padding: 10px 20px;
            background-color: #4caf50;
            color: white;
            text-decoration: none;
            border-radius: 5px;
        }
    </style>
</head>
<body>
    <div class="container">
        <div class="header">
            <h2>Task Updated</h2>
        </div>
        <div class="content">
            <p>Hello {{userName}},</p>
            <p>A task you're involved with has been updated.</p>
            {{#if taskName}}
            <p>Task Details:</p>
            <ul>
                <li>Task: {{taskName}}</li>
                {{#if projectName}}<li>Project: {{projectName}}</li>{{/if}}
                {{#if status}}<li>Status: {{status}}</li>{{/if}}
                {{#if updatedBy}}<li>Updated by: {{updatedBy}}</li>{{/if}}
            </ul>
            {{/if}}
            <p>
                <a href="{{taskUrl}}" class="button">View Task</a>
            </p>
        </div>
    </div>
</body>
</html>
