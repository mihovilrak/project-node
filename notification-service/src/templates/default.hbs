<!DOCTYPE html>
<html>
<head>
    <style>
        body { font-family: Arial, sans-serif; line-height: 1.6; }
        .container { max-width: 600px; margin: 0 auto; padding: 20px; }
        .header { background-color: #607d8b; padding: 20px; text-align: center; color: white; }
        .content { padding: 20px; }
        .button {
            display: inline-block;
            padding: 10px 20px;
            background-color: #607d8b;
            color: white;
            text-decoration: none;
            border-radius: 5px;
        }
    </style>
</head>
<body>
    <div class="container">
        <div class="header">
            <h2>Notification</h2>
        </div>
        <div class="content">
            <p>Hello {{userName}},</p>
            <p>You have a new notification.</p>
            {{#if message}}
            <p>{{message}}</p>
            {{/if}}
            {{#if taskUrl}}
            <p>
                <a href="{{taskUrl}}" class="button">View Details</a>
            </p>
            {{/if}}
        </div>
    </div>
</body>
</html>
