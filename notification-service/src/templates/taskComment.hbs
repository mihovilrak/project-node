<!DOCTYPE html>
<html>
<head>
    <style>
        body { font-family: Arial, sans-serif; line-height: 1.6; }
        .container { max-width: 600px; margin: 0 auto; padding: 20px; }
        .header { background-color: #9c27b0; padding: 20px; text-align: center; color: white; }
        .content { padding: 20px; }
        .comment-box {
            background-color: #f5f5f5;
            padding: 15px;
            border-left: 4px solid #9c27b0;
            margin: 15px 0;
        }
        .button {
            display: inline-block;
            padding: 10px 20px;
            background-color: #9c27b0;
            color: white;
            text-decoration: none;
            border-radius: 5px;
        }
    </style>
</head>
<body>
    <div class="container">
        <div class="header">
            <h2>New Comment on Task</h2>
        </div>
        <div class="content">
            <p>Hello {{userName}},</p>
            <p>A new comment has been added to a task you're watching.</p>
            {{#if taskName}}
            <p><strong>Task:</strong> {{taskName}}</p>
            {{/if}}
            {{#if commentBy}}
            <p><strong>Comment by:</strong> {{commentBy}}</p>
            {{/if}}
            {{#if commentText}}
            <div class="comment-box">
                {{commentText}}
            </div>
            {{/if}}
            <p>
                <a href="{{taskUrl}}" class="button">View Task</a>
            </p>
        </div>
    </div>
</body>
</html>
